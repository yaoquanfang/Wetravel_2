<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.swufe.wetravel">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GonglueActivity" />
        <activity android:name=".HotelActivity" />
        <activity android:name=".TicketActivity" />
        <activity android:name=".SanyaActivity" />
        <activity android:name=".TailandActivity" />
        <activity android:name=".ChengduActivity" />
        <activity android:name=".ZhuceActivity" />
        <activity android:name=".MeishiActivity" />
        <activity android:name=".JingdianActivity" />
        <activity android:name=".GouwuActivity" />
        <activity android:name=".XiuxianActivity"></activity>
    </application>

</manifest>